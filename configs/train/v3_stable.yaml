epochs: 200
lr: 1e-3
weight_decay: 0.05
label_smoothing: 0.1
clip_norm: 0.5
checkpoint_dir: outputs/models_v3
save_every: 50
class_weights: auto
use_weighted_sampler: false
patience: 50
min_delta: 0.0001
scheduler: plateau
scheduler_factor: 0.5
scheduler_patience: 15
use_focal: true
focal_gamma: 2.0
focal_boost_active_fallow: 1.5
